# Reinforcement Learning

## Model-Free Reinforcement Learning
Model-free algorithm is an algorithm that does NOT estimate transition probability distribution and reward distribution. 
Instead, it directly estimates the value function or policy function based on the experiences (state, action, reward, next state).